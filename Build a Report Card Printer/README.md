This workshop contains 10 steps.
---------------------------------------------------------------------------------------------
▫ Step 1 :
    Create a variable called name and assign it the string Alice.

▫ Step 2 :
    Print the name variable. Check the output in the terminal.

▫ Step 3 : 
    Use the type() function with name as its argument and print the output.

▫ Step 4 : 
    Declare a variable named is_student and assign it the value True.

▫ Step 5 : 
    Print both is_student and type(is_student) on the same line using a comma (,).

▫ Step 6 : 
    Remove the earlier outputs of the name variable. Then, print name and type(name) together on one line separated by a comma like the previous step.

▫ Step 7 : 
    Declare a variable named age and assign it the integer value 20. Print the value and data type of age together separated by a comma.

▫ Step 8 :
    Declare a variable named score and assign it the value 80.5. Use isinstance() to check whether score is an int, and print the result.

▫ Step 9 : 
    Replace int with float in the existing isinstance() call to confirm.

▫ Step 10 :
    Complete the report card by printing the score value along with its data type using a single print() statement.
    
Final output : 
<img width="286" height="155" alt="Screenshot (257)" src="https://github.com/user-attachments/assets/8eed77e9-dd9c-4da1-86f7-bc6db1d5c7d4" />
